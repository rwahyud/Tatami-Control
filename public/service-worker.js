/* Tatami Control — Service Worker   Cache-first untuk app shell (HTML/CSS/JS/icon) supaya bisa dipasang   sebagai PWA dan tetap terbuka saat offline. Data pertandingan   disimpan lokal (localStorage) dan tersinkron antar-tab di perangkat   yang sama. */var CACHE_NAME = "tatami-control-v19";var APP_SHELL = [  "./",  "./index.html",  "./manifest.json",  "./css/style.css",  "./js/db.js",  "./js/auth.js",  "./js/pwa.js",  "./js/app.js",  "./icons/icon-192.png",  "./icons/icon-512.png",  "./icons/icon-maskable-512.png"];self.addEventListener("install", function (event) {  event.waitUntil(    caches.open(CACHE_NAME).then(function (cache) {      return cache.addAll(APP_SHELL);    })  );  self.skipWaiting();});self.addEventListener("activate", function (event) {  event.waitUntil(    caches.keys().then(function (keys) {      return Promise.all(        keys.filter(function (k) { return k !== CACHE_NAME; }).map(function (k) { return caches.delete(k); })      );    })  );  self.clients.claim();});self.addEventListener("fetch", function (event) {  var url = event.request.url;  // Jangan cache request ke Google Fonts — selalu ambil langsung dari jaringan.  if (    url.indexOf("googleapis.com") > -1 ||    url.indexOf("fonts.g") > -1  ) {    return; // biarkan browser handle langsung (network)  }  event.respondWith(    caches.match(event.request).then(function (cached) {      if (cached) return cached;      return fetch(event.request)        .then(function (resp) {          if (event.request.method === "GET" && resp && resp.status === 200) {            var respClone = resp.clone();            caches.open(CACHE_NAME).then(function (cache) {              cache.put(event.request, respClone);            });          }          return resp;        })        .catch(function () {          if (event.request.mode === "navigate") return caches.match("./index.html");        });    })  );});